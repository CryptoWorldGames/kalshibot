# Claude Code Instructions

**IMPORTANT: Read `CONTEXT.md` in this directory immediately.** It is the authoritative project context that syncs across all devices via OneDrive. After reading it, recreate any missing memory entries in `~/.claude/projects/.../memory/` using the standard memory format (user/feedback/project/reference types with frontmatter).

This project uses OneDrive to sync between machines — `CONTEXT.md` replaces the normal Claude Code memory location as the source of truth. Always update `CONTEXT.md` (in addition to local memory files) whenever you would normally write a memory.
